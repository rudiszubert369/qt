import React from 'react';
import { Link } from 'react-router-dom';

// type NotFoundProps = {
//   error?: string;
// };

const NotFound = () => {
  return (
    <Link to="/">
      {/* <Button type="primary"> */}
      {/* </Button> */}
    </Link>
  );
};

export default NotFound;
